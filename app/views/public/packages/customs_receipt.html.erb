<%= content_for :window_title, "Generate customs receipt" %>
<p>This will generate a customs receipt for package <%= @package.hc_id %> and email it to you at <%= @package.recipient_email %>.</p>
<%= button_to "do it!", package_generate_customs_receipt_path(@package), method: :post %> 